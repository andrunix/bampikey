{
  "name": "bampikey",
  "version": "1.0.0",
  "description": "Makes call to 'Spring MVC' page and gets the BAMPI value",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Pierce",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.1",
    "request": "^2.81.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "eslint": "^4.2.0"
  }
}
